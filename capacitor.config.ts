import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'local.acgplan.mobile',
  appName: '绯典阁',
  webDir: 'dist',
  android: {
    backgroundColor: '#121017',
    zoomEnabled: false,
  },
};

export default config;
